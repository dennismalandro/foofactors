---
# title: 
output:
  html_document:
    fig_width: 5
    fig_height: 5
    fig_caption: true
    # code_folding: hide
    # css: ~/../Dropbox/Software/R/css/smaller-headers.css
    css: ~/../Documents/smaller-headers.css
---

```{r setup, include=F}
knitr::opts_chunk$set(
  comment = NA,
  echo = FALSE, 
  include = TRUE,
  message = FALSE,
  warning = FALSE)
library(devtools)
library(fs)
library(tidyverse)
```

```{r stop-scrollin}

create_package(getwd())

dir_info() %>% select(path, type)

use_git()

dir_info(all = TRUE, regexp = "^[.]git$") %>% 
  select(path, type)
#> # A tibble: 1 × 2
#>   path       type     
#>   <fs::path> <fct>    
#> 1 .git       directory

a <- factor(c("character", "hits", "your", "eyeballs"))
b <- factor(c("but", "integer", "where it", "counts"))

c(a, b)

use_r('fbind')

load_all()

fbind(a, b)

check()

use_mit_license('Dennis Malandro')
document()

```

```{r stop-scrollin}

```
 
